# 网页信息采集 Agent：业务逻辑决策日志

> 本文件是当前讨论的唯一决策基线。每次确认的决定都追加记录；后续变更必须显式标记“替代了哪个决定”，不得静默覆盖。

## 日志规则

- 【已确认】：用户已经明确确认，可以进入最终设计。
- 【待讨论】：尚未决定，不得作为实现假设。
- 【已作废】：禁止继续用于新设计。
- 每条决定使用稳定编号，方便在后续规格、计划和测试中引用。

## D-000：历史方案整体作废

- 状态：已确认
- 日期：2026-08-03
- 决定：本轮讨论之前形成的架构、技术选型、业务流程、参数默认值和可视化页面全部作废。
- 影响：旧内容不得作为新方案的需求来源；新方案只能引用本文件中从 D-001 开始重新确认的决定。

## D-001：当前需求基线

- 状态：已确认
- 日期：2026-08-03
- 产品：专精于爬取和搜集网页信息的 Agent。
- 模型接入：用户使用自己的 API Key，接入当前主流 AI 模型。
- 核心产物：根据用户需求整理采集结果，并导出 CSV 文件。
- 前端：Vue 3。
- 后端：尚未决定，需根据业务逻辑和框架调研结果选择。
- 框架要求：调研 GitHub 上相关开源 Agent 框架，再根据本项目业务做裁剪和微调。

## D-002：业务逻辑讨论维度

- 状态：已确认
- 日期：2026-08-03
- 必须逐项讨论：
  1. 目标理解
  2. 任务规划
  3. 工具调用与执行
  4. 状态管理
  5. 上下文与记忆
  6. 错误处理与自我纠错
  7. 结果验证
  8. Checkpoint 与中断恢复
  9. 幂等性
  10. 人工确认
  11. 权限控制
  12. 可观测性
  13. 用户取消与暂停

## 当前待讨论

- 设计确认第 5 部分：来源发现、网页抓取与错误恢复。

## D-003：同时支持两类采集目标

- 状态：已确认
- 日期：2026-08-03
- 维度：1. 目标理解
- 决定：同时支持“探索式搜集”和“指定来源抽取”。
- 业务含义：
  - 探索式搜集：用户给出主题和字段，Agent 自行搜索、发现并筛选来源。
  - 指定来源抽取：用户提供网站、栏目或 URL，Agent 分析页面并批量抽取。
  - 混合任务：允许先通过搜索发现目标网站，再进入指定来源的批量抽取。
- 实现约束：目标理解阶段必须先识别任务类型，后续任务规划不得用同一套固定流程处理所有任务。

## D-004：采集任务规格采用分级确认和版本冻结

- 状态：已确认
- 日期：2026-08-03
- 维度：1. 目标理解
- 决定：新任务默认先让用户确认结构化的 `CollectionSpec`；使用已保存模板，或输入信息完整且判断置信度足够高时，可以自动开始执行；低置信度或存在关键歧义时必须澄清或确认。
- 版本规则：任务一旦开始执行，本次 `CollectionSpec` 版本即冻结；用户后续调整目标时创建新版本，保留旧版本和变更历史，不在原版本上静默覆盖。
- 影响：既能避免 Agent 理解错误后大规模误采集，也不会让重复性、标准化任务每次都被确认步骤打断。

## D-005：CSV 核心字段冻结，可选字段受控扩展

- 状态：已确认
- 日期：2026-08-03
- 维度：1. 目标理解
- 决定：任务规格确认后，CSV 的核心列名称、类型和含义保持冻结；Agent 在执行中发现稳定且有价值的新字段时，可以提出新增可选列。
- 审批规则：新增可选列默认需要用户批准；用户可在任务规格中预先启用“自动扩展字段”，允许 Agent 按预设条件自动增加可选列。
- 一致性规则：字段结构的每次变化都产生新版本；同一结构版本下的所有数据行必须遵守同一字段定义，缺失字段显式记录为空值，不允许因页面差异造成列错位。
- 影响：CSV 可以稳定用于数据库导入和后续自动化处理，同时不会完全丢弃采集过程中发现的额外高价值信息。

## D-006：使用多条件完成模型，并区分正常完成和部分完成

- 状态：已确认
- 日期：2026-08-03
- 维度：1. 目标理解
- 决定：任务完成不能仅由固定记录数或 Agent 的主观判断决定，而由采集范围、结果质量、信息饱和度和资源预算共同控制。
- 定向采集：任务范围内的页面全部成功处理，或对未处理页面记录了明确原因并完成允许次数的重试后，才满足范围完成条件。
- 探索式采集：达到任务规格规定的最低合格记录数，并且连续若干轮搜索新增的合格、去重记录低于饱和阈值后，才满足探索完成条件。
- 资源边界：所有任务必须配置页面数量、运行时间和模型费用等预算上限；具体阈值属于任务规格或系统策略，不在本决定中写死。
- 终态规则：达到业务目标和质量条件时标记为“正常完成”；因预算耗尽、超时、用户停止或其他限制结束时标记为“部分完成”，不得伪装成完整结果。
- 输出要求：CSV 之外必须提供完成度、未覆盖范围、失败项目和停止原因，使用户能够判断结果是否适合使用。

## D-007：采用上层受约束、下层可自适应的分层计划

- 状态：已确认
- 日期：2026-08-03
- 维度：2. 任务规划
- 决定：计划分为业务约束层和执行策略层。已确认的目标、字段、范围、预算、质量要求与完成标准属于业务约束层，Agent 无权自行修改；搜索词、来源顺序、分页方式、抓取工具与任务批次属于执行策略层，允许 Agent 根据执行证据动态调整。
- 重规划触发：来源失效、连续失败、重复率过高、结果质量持续不达标或发现更优来源时，可以重新规划执行策略层。
- 审批边界：任何会扩大采集范围、提高预算、改变字段含义或降低质量标准的调整，必须创建新的规格版本并获得用户确认。
- 审计要求：每次重规划必须记录触发原因、证据、旧计划、新计划和影响范围，不得静默改变执行路径。

## D-008：使用类型化标准任务节点组装可验证的执行图

- 状态：已确认
- 日期：2026-08-03
- 维度：2. 任务规划
- 决定：系统提供经过注册的标准任务节点，例如来源搜索、访问规则检查、链接发现、网页下载、浏览器渲染、字段提取、标准化、去重、质量验证和 CSV 生成；Agent 根据任务类型选择、跳过、连接这些节点并填写参数。
- 权限边界：Agent 不得直接创造未注册的可执行动作；新工具必须先注册为具有明确契约的任务节点，才能进入计划。
- 节点契约：每种节点必须声明输入、输出、前置条件、超时、重试策略、资源预算、权限等级、幂等键和可恢复边界。
- 计划校验：执行前由非大模型的规则校验器检查节点类型、依赖关系、参数、预算和权限是否合法；非法计划不得开始执行。
- 影响：执行计划可以表示为可追踪的有向任务图，从而支持节点级重试、暂停、恢复、审计和测试。

## D-009：抓取工具按成本和能力逐级升级

- 状态：已确认
- 日期：2026-08-03
- 维度：3. 工具调用与执行
- 决定：网页获取采用固定的能力升级阶梯，优先检查公开 API、RSS、站点地图和结构化数据，其次使用普通 HTTP 请求与 HTML 解析，再按需升级到无头浏览器渲染，最后才使用能够点击、滚动和交互的浏览器 Agent。
- 升级条件：只有在当前层级返回正文为空、关键字段缺失、检测到动态加载或必须交互等可验证证据时，才能升级到成本更高的工具；不能仅凭大模型猜测反复升级。
- 受限操作：登录、验证码和其他受限行为不得自动绕过，必须转入人工处理、等待授权或明确记录为不可处理。
- 策略复用：成功抓取后保存站点级工具策略和有效期；同站后续页面优先复用，并在页面结构变化或策略失效时重新探测。
- 影响：静态页面保持高吞吐和低成本，同时为动态或交互式页面保留可控的处理能力。

## D-010：规则优先提取，大模型处理语义不确定部分

- 状态：已确认
- 日期：2026-08-03
- 维度：3. 工具调用与执行
- 决定：字段提取采用分级策略，优先读取 JSON-LD、Meta、表格等结构化数据，其次复用已验证的 CSS/XPath 或站点模板规则；仅在规则无法覆盖、字段含义模糊或页面结构不统一时调用大模型进行结构化提取。
- 校验边界：大模型输出必须符合任务字段的名称、类型、枚举和格式约束，并由普通程序校验；校验失败的结果不能直接写入最终数据集。
- 规则学习：从大模型结果推导出的站点规则必须经过代表性页面抽样验证，达到质量阈值后才能用于批量页面；规则需要版本号并可回退。
- 证据要求：每个字段保留来源 URL、原文或 DOM 定位、提取方式、提取器版本和置信度，使最终值可追溯。
- 影响：大模型集中处理理解问题，确定性程序承担批量执行，降低用户自带 API Key 的消耗并提高结果可复现性。

## D-011：使用分层业务状态和追加式状态事件

- 状态：已确认
- 日期：2026-08-03
- 维度：4. 状态管理
- 决定：系统分别管理任务、规格与计划版本、执行节点、URL、数据记录和文件产物的状态，不能只用一个任务进度或依赖文字日志推断业务状态。
- 当前状态：为各层对象保存可快速查询的当前状态，例如任务的待确认、排队、运行、暂停中、已暂停、等待审批、正常完成、部分完成、失败和已取消；执行节点另有等待、可执行、执行中、等待重试、成功、跳过、阻塞、失败和取消等状态。
- 历史记录：每次状态转换都追加为不可覆盖的事件，至少包含发生时间、操作者、转换前后状态、原因、关联节点和运行编号。
- 写入边界：大模型不得直接写数据库状态，只能提交结构化命令；状态机校验当前状态、允许的转换、版本和权限后，以事务方式更新当前状态并追加事件。
- 影响：断点恢复、幂等执行、人工审批、取消控制和审计都基于同一套可信状态模型，而不是依赖易丢失的对话上下文。

## D-012：上下文与记忆分层存储、按需检索并严格隔离

- 状态：已确认
- 日期：2026-08-03
- 维度：5. 上下文与记忆
- 决定：使用四层记忆模型：不可变任务记忆保存已确认的任务规格、字段和计划版本；短期工作记忆保存当前阶段所需的结构化摘要；证据存储保存原始网页、正文、截图和字段证据；可复用经验记忆保存已验证的站点策略、提取规则、任务模板和字段映射。
- 上下文装配：每次模型调用只按任务、节点和证据 ID 检索必要片段，不重复发送完整对话或所有网页；模型生成的摘要只是可重建缓存，数据库状态和原始证据才是事实来源。
- 经验有效性：可复用经验必须带有适用域名、规则版本、验证时间、失效时间和质量指标；过期或检测到站点结构变化后必须重新验证。
- 隔离边界：不同用户和项目默认严格隔离；新任务仅使用当前项目内已验证经验，跨项目共享需要用户或管理员明确授权。
- 密钥边界：API Key、Cookie、密码等秘密不得写入提示词、普通日志或长期记忆，只能通过受控的密钥引用在执行时临时取用。
- 影响：在控制模型上下文和费用的同时复用可靠经验，并避免旧任务信息污染新任务或跨用户泄漏。

## D-013：错误分类后执行有界重试与证据驱动的自我纠错

- 状态：已确认
- 日期：2026-08-03
- 维度：6. 错误处理与自我纠错
- 决定：系统先将错误归类，再执行对应的恢复策略；不得对所有错误使用相同重试方式，也不得让大模型无限自由尝试。
- 分类策略：网络超时、临时服务异常和限流使用带随机抖动的指数退避；结构或提取失败可更换提取器、重新分析样本或按 D-009 升级工具；密钥无效、额度不足、登录失效等错误立即停止相关节点并等待用户处理；域名持续失败时触发熔断；质量不达标时分析样本并调整下层策略，但不能降低已确认标准。
- 纠错约束：同一种失败只有在输入、工具、参数或外部环境至少一项发生有效变化时才能再次尝试；每次纠错必须记录假设、修改内容和验证结果。
- 预算边界：节点、URL、域名和任务分别设置重试与资源预算；预算耗尽后进入阻塞或失败状态，保留最后错误、尝试历史和人工处理建议。
- 影响：允许系统从临时故障和页面变化中自动恢复，同时防止死循环、费用失控和对目标网站的持续无效请求。

## D-014：使用多层验证门禁和三类结果分区

- 状态：已确认
- 日期：2026-08-03
- 维度：7. 结果验证
- 决定：记录必须依次通过结构与类型、必填完整性、字段证据、业务规则、去重、跨来源冲突和分层抽样验证，不能仅凭格式正确或大模型主观判断进入最终结果。
- 结果分区：验证结果分为“已通过”“待复核”“已拒绝”；默认正式 CSV 只包含已通过记录，待复核数据可单独导出，已拒绝数据保留拒绝原因和证据但不得混入正式 CSV。
- 冲突规则：不同来源存在冲突时，根据任务定义的来源优先级、证据强度和更新时间进行确定性裁决；无法可靠裁决的记录进入待复核区，不允许静默选择一个值。
- 抽样规则：按照来源、提取方式、规则版本和置信度分层抽样，防止总体随机抽样遗漏某个系统性错误来源；抽样失败可阻止对应批次导出并触发重新处理。
- 质量产物：最终任务同时生成质量报告，至少包含通过率、缺失率、重复率、冲突数、来源覆盖、抽样准确率、待复核数和拒绝数。
- 影响：CSV 默认提供可直接使用的可信数据，同时完整保留有争议或不合格记录的审计线索。

## D-015：在节点边界和小批次事务中建立可验证的 Checkpoint

- 状态：已确认
- 日期：2026-08-03
- 维度：8. Checkpoint 与中断恢复
- 决定：每个标准任务节点开始前持久化输入、节点版本和运行编号；链接发现、抓取、提取、验证等长流程按小批次提交。只有业务数据、当前状态和状态事件在同一事务中成功写入后，该批次才形成有效检查点。
- 长任务处理：浏览器操作和大文件下载等长任务定期发送心跳并报告进度；心跳用于判断执行者是否失联，但未提交的临时进度不冒充已完成结果。
- 恢复方式：崩溃、重启或暂停后从最后一个已提交批次继续；检查点保存数据库对象或文件产物的 ID、内容校验值和版本，不依赖不可恢复的大模型内部思考过程。
- 有效性检查：恢复前核对任务规格、字段结构、计划节点、提取器、工具和原始证据版本；兼容的中间结果直接复用，失效部分及其受影响下游选择性重算。
- 版本边界：修改任务目标时创建新规格版本；旧检查点作为历史保留，但未经兼容性判定不得用于新版本。
- 影响：在 Worker 崩溃、服务器重启和用户暂停后最大限度保留有效进度，同时避免混用过期中间结果。

## D-016：使用端到端幂等键、事务写入和外部动作回执

- 状态：已确认
- 日期：2026-08-03
- 维度：9. 幂等性
- 决定：系统允许队列和基础设施重复投递任务，但通过端到端幂等机制保证同一业务动作只产生一次有效结果。
- 身份规则：用户命令携带请求幂等键；执行节点使用任务规格版本、节点类型和输入指纹生成幂等键；URL 规范化后标识页面，内容哈希判断页面是否真实变化；提取结果结合任务版本、URL、字段版本和提取器版本进行更新或复用。
- 数据规则：任务定义业务唯一键，数据使用带版本校验的更新写入而非盲目追加；数据库唯一约束作为最后防线，但不替代节点级幂等设计。
- 产物规则：CSV 等文件由数据集版本和内容校验值标识，相同数据版本复用已有产物，不生成多个含义相同的文件。
- 事务规则：业务数据、状态变化和待执行事件尽量通过事务性发件箱在同一数据库事务内提交，再由独立发送器执行外部动作。
- 外部副作用：邮件、Webhook、发布和外部数据库写入优先传递对方支持的幂等键，并保存请求编号与回执；若外部结果未知且对方不支持幂等，停止自动重试并请求人工确认。
- 影响：任务在崩溃恢复和重复调度后仍可安全重放，不会重复写入、重复导出或重复触发外部副作用。

## D-017：人工确认采用风险分级和有范围、有期限的授权

- 状态：已确认
- 日期：2026-08-03
- 维度：10. 人工确认
- 决定：公开网页在已确认范围、字段和预算内的读取与处理属于低风险，可自动执行；浏览器 Agent、显著增加请求或使用更昂贵模型等中风险动作按用户预设限额决定是否确认；凭据访问、非公开页面、扩大域名范围、提交表单、向第三方发送或写入数据、发布、邮件和付费动作属于高风险，必须获得明确审批。
- 禁止边界：绕过验证码、规避访问控制、窃取凭据或其他系统安全策略禁止的行为，不能通过普通任务审批临时放行。
- 审批内容：请求必须展示动作、目标、原因、将发送或修改的数据、预计费用、副作用、授权范围和失效时间，秘密值只显示脱敏引用。
- 授权粒度：用户可批准仅本次动作、参数完全相同的一批动作，或任务内明确限定的范围；不得使用模糊确认获得跨任务、永久或无限范围授权。
- 失效规则：审批绑定任务规格版本和参数指纹；目标、数据、费用、范围或规格版本变化后自动失效。用户可随时撤销尚未消费的授权。
- 影响：正常采集保持自动化，高风险副作用则始终处于用户知情且可审计的授权边界内。

## D-018：产品采用单用户模式，不建设多用户权限体系

- 状态：已确认
- 日期：2026-08-03
- 维度：11. 权限控制
- 决定：产品只服务一个用户，不设计租户、组织、项目成员、角色、邀请、协作、资源共享和跨用户数据隔离等功能。
- 简化结果：不建设 RBAC/ABAC 用户授权体系，不区分所有者、管理员、操作员、审核员和只读用户，前后端均不出现团队成员或角色管理页面。
- 保留边界：仍保留 Agent 执行安全控制，包括危险工具默认禁用、网络目标限制、任务文件隔离、密钥不进入模型上下文，以及 D-017 规定的高风险操作人工确认。这些约束用于限制自动程序的行为，不是用户权限系统。
- 影响：显著降低账号、权限、数据隔离和前端管理复杂度；如果未来需要多人协作，应作为独立版本重新设计，而不是在当前版本预埋复杂权限模型。

## D-019：采用开发期本机运行、正式期服务器部署的单用户模式

- 状态：已确认
- 日期：2026-08-03
- 维度：11. 权限控制与部署边界
- 决定：开发阶段在本机运行，服务默认只监听本机地址且不建设登录流程；产品成熟后部署到服务器，仍然只供同一个用户远程使用，不因此引入多用户、团队或角色体系。
- 架构约束：业务接口、任务状态和文件存储从一开始避免依赖浏览器本机路径，使后续可以将前端、API、Worker、数据库和文件存储迁移到服务器；开发阶段不必提前实现完整生产运维功能。
- 上线门禁：切换到服务器模式前必须补齐 HTTPS、访问保护、密钥持久化、备份恢复、网络出口限制和生产配置校验；开发模式的“无登录且只监听本机”配置不得直接用于公网。
- 影响：当前迭代保持简单，同时避免未来服务器部署需要重写核心业务逻辑。

## D-020：开发模式和服务器模式都必须登录，提供独立注册页与登录页

- 状态：已确认
- 日期：2026-08-03
- 维度：11. 权限控制与身份认证
- 替代关系：本决定替代 D-019 中“开发阶段不建设登录流程”的部分；D-019 的本机开发、服务器上线和单用户原则继续有效。
- 决定：无论开发环境还是生产环境，未认证访问都不能进入业务页面；Vue 3 前端需要独立的注册页面和登录页面，后端提供相应身份认证接口与会话校验。
- 单用户边界：注册和登录只用于保护唯一用户的身份，不引入团队、角色、邀请或多用户协作体系。
- 影响：身份认证从第一阶段即进入产品范围，开发环境和生产环境使用一致的业务访问门禁，减少上线时再改造全部接口的风险。

## D-021：服务器通过公网域名访问并使用应用内置认证

- 状态：已确认
- 日期：2026-08-03
- 维度：11. 权限控制与部署边界
- 决定：正式服务器允许通过公网域名访问，不要求用户先进入 VPN 或私有网络；应用自身负责单用户注册、登录和会话保护。
- 上线要求：生产环境必须启用 HTTPS、安全 Cookie、跨站请求防护、登录限流、密码安全存储、会话失效和可信反向代理配置；敏感接口不能因处于单用户模式而跳过认证。
- 影响：用户可以从外部网络直接使用系统，但生产部署和身份认证将成为正式上线的安全门禁。

## D-022：支持多个注册账号，但所有账号只有统一的“用户”角色

- 状态：已确认
- 日期：2026-08-03
- 维度：11. 权限控制与身份认证
- 替代关系：本决定整体替代 D-018 的“单用户模式”，替代 D-019、D-020、D-021 中所有“仅一个账号”或“唯一用户”的表述；本机开发后部署服务器、开发和生产均需认证、提供独立注册与登录页面、服务器通过公网域名访问等决定继续有效。
- 决定：系统允许多个用户自行注册账号并登录，但所有账号只有同一种 `user` 角色，不设置管理员、所有者、操作员、审核员或只读用户等角色。
- 功能边界：不建设角色分配、权限组、成员邀请、管理员后台和人工账号审批流程；注册入口不会因为已有账号而自动关闭。
- 权限原则：所有已登录用户拥有相同的产品功能权限；是否能访问某项数据由数据归属关系决定，而不是由不同角色决定。
- 影响：保留简单一致的权限模型，同时必须可靠实现账号认证、数据所有权校验和跨用户隔离，不能再按单账号应用处理数据库和文件路径。

## D-023：不同用户的全部业务数据完全私有隔离

- 状态：已确认
- 日期：2026-08-03
- 维度：11. 权限控制
- 决定：每个用户只能访问自己拥有的任务、任务规格、API Key、Cookie、采集证据、原始网页、提取规则、任务模板、质量报告和 CSV 等资源；当前版本不提供用户之间的数据共享或协作功能。
- 校验方式：所有业务数据必须带有不可为空的用户归属标识；后端在每次读取、修改、导出、恢复和删除时根据当前登录用户强制校验归属，不能依赖前端隐藏数据。
- 执行隔离：Worker 领取任务后只获得该任务所属用户的资源引用；数据库查询、对象存储路径、缓存键、日志查询和文件下载都必须包含用户边界。
- 密钥隔离：用户提供的模型 API Key、Cookie 和其他凭据独立加密保存，只能用于该用户明确授权的任务，不允许跨用户复用。
- 功能边界：当前版本不共享用户自定义的网站规则、模板或历史经验；系统内置的公共连接器和静态规则属于产品代码，不属于任何用户数据。
- 影响：所有账号功能相同但数据互不可见，避免公网注册场景中的任务、密钥和采集结果泄漏。

## D-024：使用用户时间线和脱敏的端到端诊断追踪

- 状态：已确认
- 日期：2026-08-03
- 维度：12. 可观测性
- 决定：可观测性分为用户可见的任务时间线和开发诊断追踪。时间线展示当前阶段、各类记录计数、计划调整、工具升级、重试与熔断、审批、预算消耗、暂停恢复和停止原因；诊断追踪记录任务、节点、执行尝试、模型调用和工具调用之间的关联。
- 追踪字段：至少记录统一追踪编号、规格与计划版本、工具或模型名称、Token 与预计费用、耗时、响应状态、错误分类、重试次数、输入输出大小和证据引用。
- 隐私边界：API Key、密码、Cookie、认证头和表单秘密不得进入日志；原始网页与完整模型输入输出存入用户私有证据存储，普通日志仅保存脱敏摘要和证据 ID。
- 用户隔离：时间线、日志、指标、证据和脱敏诊断包都必须执行 D-023 的数据所有权校验，用户只能查看和导出自己的内容。
- 影响：用户能够理解 Agent 做了什么、为什么调整和花费多少，开发者也能定位具体失败链路，同时避免日志成为秘密和用户数据的泄漏渠道。

## D-025：暂停与取消采用协作式停止，并设置超时强制回收

- 状态：已确认
- 日期：2026-08-03
- 维度：13. 用户取消与暂停
- 暂停：收到请求后立即停止调度新节点和新批次，向运行节点发送暂停信号；节点在完成当前最小安全单元后提交检查点并退出。超过暂停超时仍未响应时回收 Worker，未提交部分依据幂等规则重新处理。任务到达“已暂停”后可从有效检查点恢复。
- 取消：立即禁止新工作并尝试中止网络、浏览器和模型调用；保留已提交的数据、证据与草稿产物，任务标记为“已取消”并说明部分结果。取消后的原运行不能直接恢复，继续采集时创建新的运行并显式复用兼容结果。
- 改向：用户修改目标、字段、范围或预算前先安全暂停；修改形成新的任务规格版本，系统重新判断旧结果兼容性并生成新计划。
- 一致性：前端在 Worker 尚未停止时显示“暂停中”或“取消中”，只有所有运行节点退出或超时回收后才显示最终状态；已经完成的外部副作用不得宣称已自动撤销。
- 影响：用户操作能够及时生效，同时保护事务、Checkpoint 和产物一致性，避免强杀造成不可恢复损坏。

## D-026：选择 Pydantic AI + Temporal 的 Python 后端方案

- 状态：已确认
- 日期：2026-08-03
- 选择证据：用户在终端选择 A；可视化页面最终点击选择也为 A。
- 前端：Vue 3。
- API 层：FastAPI，负责注册登录、数据所有权校验、任务命令、审批、查询、下载和 SSE 事件流。
- Agent 层：Pydantic AI，负责把自然语言转换为类型化任务规格、生成受约束计划、选择已注册工具、结构化提取和语义纠错；不作为业务状态数据库或批量爬虫队列。
- 可靠执行层：Temporal Python SDK，负责长任务编排、Activity 重试与心跳、暂停恢复、取消、人工审批等待和崩溃恢复；所有网络、浏览器、模型和文件副作用放入 Activity。
- 业务数据层：PostgreSQL 保存用户、任务规格与版本、计划、节点运行、URL、记录、证据索引、审批、幂等键、状态事件和质量指标，是业务事实来源。
- 文件层：使用 S3 兼容对象存储保存原始网页、截图、正文、诊断包和 CSV；本地开发使用 MinIO，服务器可继续使用 MinIO 或替换为兼容云存储。
- 网页工具层：静态页面优先使用自建 HTTP 适配器，确定性批量爬取使用 Scrapy，动态渲染使用 Crawl4AI/Playwright，复杂交互浏览器 Agent 仅作为最后一级可选工具。
- 可观测性：统一使用 OpenTelemetry 关联 API、Temporal Workflow/Activity、模型和抓取工具。
- 排除项：不同时引入 LangGraph，避免 Temporal Workflow 状态与 LangGraph Checkpoint 成为两个流程事实来源；不采用全 TypeScript 后端。
- 影响：部署中增加 Temporal 服务与 Worker，但 D-013、D-015、D-016、D-017、D-024、D-025 所要求的可靠性边界由专门工作流底座承载，减少自研恢复语义。

## D-027：设计第 1 部分“总体架构与组件职责边界”通过

- 状态：已确认
- 日期：2026-08-03
- 决定：用户通过由 Vue 3、FastAPI、Temporal、Pydantic AI、分级网页工具、PostgreSQL、S3 兼容对象存储和 OpenTelemetry 组成的总体架构与职责边界。
- 状态权威：Temporal 历史负责执行位置与恢复；PostgreSQL 负责用户归属和对外业务状态；两者通过带幂等键的 Activity 和追加事件同步，不在同一语义上设置两个事实来源。

## D-028：设计第 2 部分“任务完整生命周期”通过

- 状态：已确认
- 日期：2026-08-03
- 决定：用户通过从任务草稿、目标理解、规格确认、计划校验、Temporal 启动、分批采集、持续验证、受控重规划、完成判定到 CSV 与质量报告生成的完整生命周期设计。
- 路径差异：探索式、定向式和混合式任务使用不同的前置阶段，但统一进入类型化批次采集、证据保存、验证和导出流程。

## D-029：设计第 3 部分“BYOK 多模型接入”通过

- 状态：已确认
- 日期：2026-08-03
- 接入：首批内置 OpenAI、Anthropic、Google Gemini、DeepSeek、OpenRouter、Ollama 和自定义 OpenAI-compatible 适配器；其他协议通过注册新适配器扩展。
- 配置：用户可保存多个私有模型配置；任务默认共用一个配置，高级模式可分别指定规划、提取和验证模型。任务开始后冻结模型配置版本。
- 密钥：API Key 信封加密存储，主密钥与数据库分离；前端不可回读明文，Temporal 历史和日志仅使用密钥引用，Activity 执行时临时解密。
- 切换：认证、额度或模型故障不会触发未授权的模型替换；只有用户预先确认的备用顺序、费用和适用步骤才能自动切换。

## D-030：设计第 4 部分“数据模型、Checkpoint 与幂等性”通过

- 状态：已确认
- 日期：2026-08-03
- 数据对象：用户、模型凭据与配置、任务与规格版本、运行与计划版本、节点运行与尝试、URL 与网页快照、记录与字段证据、审批、产物、领域事件、幂等键和事务性发件箱形成核心数据模型。
- 写入：业务状态只通过带乐观锁的状态机事务改变，同时更新当前状态、追加事件和待发送事件；大模型与抓取工具不能直接写状态。
- Checkpoint：对象内容先按哈希上传，URL、记录、证据引用、节点进度和事件在 PostgreSQL 小批次事务中提交，成功后生成 `checkpoint_id`；Temporal 心跳不替代数据库 Checkpoint。
- 幂等：API、Workflow、节点批次、网页快照、提取结果、CSV 和外部动作分别使用稳定幂等身份，Activity 重试复用已提交结果。

# UI/UX 页面与交互设计决策

> 本节记录 2026-08-10 已确认的页面划分、导航、核心交互、Drawer/Modal、路由与业务逻辑绑定。  
> 后续如有修改，继续使用新的稳定编号并显式声明替代关系，不静默覆盖。

## D-031：产品主交互采用 Agent 对话型

- 状态：已确认
- 日期：2026-08-10
- 决定：产品主体验采用 Agent 对话型，而不是传统功能后台型。
- 业务边界：对话只是用户交互主线，`Task`、`CollectionSpec`、`Plan`、`Run`、`Record`、`Evidence`、`Approval`、`Artifact` 等仍然是结构化业务对象，不能降级成纯聊天文本。
- 影响：前端应让用户感觉是在持续与 Agent 协作，但规格、状态、审批、质量、证据、导出仍由后端状态机和持久化数据驱动。

## D-032：主工作区采用“Agent 对话 + Overlay 任务状态 Drawer”

- 状态：已确认
- 日期：2026-08-10
- 决定：Agent 对话区占据主工作空间；任务实时状态使用右侧 Drawer。
- Drawer 规则：
  - 默认关闭，不占页面宽度。
  - 打开时覆盖在当前页面上，不挤压、不重排底层布局。
  - 在对话、数据、质量、执行详情和证据查看器页面均可打开。
- Drawer 展示：
  - 任务名称、任务状态、当前阶段。
  - URL 发现/处理数量。
  - 已通过、待复核、已拒绝数量。
  - `Spec Version`、`Plan Version`。
  - 待审批数量。
  - 暂停、取消、执行详情入口。
- 业务绑定：
  - 暂停 → D-025 协作式暂停。
  - 取消 → D-025 取消流程。
  - 待审批 → Approval Drawer。
  - 查看执行详情 → `/tasks/:taskId/execution`。

## D-033：一个 Agent 对话就是一个 Task

- 状态：已确认
- 日期：2026-08-10
- 决定：前端不再区分 Chat 和 Task 两个用户概念。
- 结构：
  - 一个 Task 对应一个持续 Agent 对话工作区。
  - 一个 Task 可拥有多个 `CollectionSpec Version`。
  - 一个 Task 可拥有多个 `Plan Version`。
  - 一个 Task 可拥有多个 Run。
- 默认入口：点击 Task 名称或主体统一进入 `/tasks/:taskId/chat`。

## D-034：新任务采用“自然语言输入 + 网页采集快捷入口”

- 状态：已确认
- 日期：2026-08-10
- 决定：新任务不是完全空白聊天页，而是自然语言输入框加网页采集快捷入口。
- 默认示例文案：使用中国本地业务场景，例如“帮我搜集深圳的工业自动化设备供应商，获取公司名、官网、主营产品和联系方式”。
- 快捷入口：
  - 搜集某类信息。
  - 抓取指定网站。
  - 搜索并批量抓取。
  - 添加网址。
  - 使用模板。
- 业务规则：
  - 快捷入口不是三套独立 Workflow，只是形成 Task Draft 的辅助方式。
  - “添加网址”只写入 Draft Context，不立即抓取。
  - Agent 仍按 D-003 识别探索式、指定来源式或混合式任务。

## D-035：`CollectionSpec` 使用摘要确认卡 + 完整编辑器

- 状态：已确认
- 日期：2026-08-10
- 决定：聊天中展示可快速阅读的 `CollectionSpec` 摘要卡；“查看 / 修改采集方案”打开完整 Editor Sheet/Modal。
- 摘要卡：
  - 任务名称。
  - 任务类型。
  - Agent 理解的目标。
  - 字段列表。
  - 采集范围。
  - 完成条件摘要。
  - Spec 版本。
- 完整编辑器：
  - 目标。
  - 字段定义：名称、类型、必填/可选。
  - 添加字段。
  - Agent 可提出新增字段。
  - 自动扩展字段开关。
  - 采集范围。
  - 完成标准。
  - 高级运行设置。
- “确认并执行”：
  - 校验 Spec。
  - 创建并冻结 `CollectionSpec Version`。
  - 生成 Plan。
  - 规则校验。
  - 启动 Temporal Workflow。
- 约束：执行中的 Spec 不静默修改，后续变更必须产生新版本，继续遵循 D-004/D-005。

## D-036：当前版本取消所有金额预算、平台收费和金额型费用 UI

- 状态：已确认
- 日期：2026-08-10
- 替代关系：
  - 替代 D-006 中“模型费用预算上限”作为当前版本任务规格必填资源边界的部分。
  - 替代 D-017 中审批 UI 必须展示“预计费用”的部分。
  - 替代 D-024 中用户时间线必须展示“预算消耗”和“预计费用”的部分。
  - D-029 的模型备用顺序继续存在，但当前产品不以金额预算作为 UI 控制条件。
- 决定：当前版本不建设平台收费、套餐、余额、充值、账单、支付或金额预算系统。
- 当前 UI 不展示人民币/美元任务预算、模型费用上限或预计费用。
- 保留：
  - BYOK 模型接入。
  - Token、请求次数、输入输出大小、模型调用次数等技术统计。
  - 最大网页数、最长运行时间、并发、重试次数等非金额型运行限制。

## D-037：运行限制默认放入“高级运行设置”

- 状态：已确认
- 日期：2026-08-10
- 决定：最大处理网页数、最长运行时间、单 URL 最大重试次数、同域并发等默认折叠。
- 原则：普通用户先关注“采什么、字段、范围、完成条件”，而不是先配置爬虫技术参数。
- 这些限制仍进入 D-013 的有界重试和任务安全边界。

## D-038：Spec 确认后自动执行合法 Plan，越界或高风险才审批

- 状态：已确认
- 日期：2026-08-10
- 决定：用户确认 Spec 后，Agent 生成 Plan；规则校验通过后自动启动执行，不要求每次二次确认 Plan。
- Plan 仍可查看。
- 只有越界/高风险动作才打断：
  - 使用网站凭据访问非公开页面。
  - 扩大域名范围。
  - 提交表单。
  - 对第三方写入数据。
  - 其他 D-017 明确要求审批的动作。
- 禁止行为不能通过审批放行。

## D-039：聊天只显示重要事件，细粒度日志进入执行详情

- 状态：已确认
- 日期：2026-08-10
- 聊天显示：
  - 阶段变化。
  - 重要异常。
  - 工具升级。
  - 策略调整。
  - 审批。
  - 信息饱和判断。
  - 完成/部分完成。
- 聊天不显示大量单 URL 普通日志、HTTP 200、Checkpoint 编号等。
- 信息层级：
  - Chat：发生了什么重要事情。
  - Task Drawer：任务现在怎么样。
  - Execution：系统具体怎么跑。

## D-040：任务运行中允许实时查看已提交结果

- 状态：已确认
- 日期：2026-08-10
- 决定：Task 未结束时可以进入数据页查看实时增长的数据。
- 只展示已经形成有效数据库 Checkpoint 的业务数据。
- FastAPI 通过 SSE 推送状态和记录变化。
- 默认优先展示“已通过”结果，避免把未经验证的数据误当最终结果。

## D-041：数据采用表格 + Overlay Record Drawer + 字段证据

- 状态：已确认
- 日期：2026-08-10
- 结构化结果默认使用全宽表格。
- 点击一条记录打开右侧 Overlay Record Detail Drawer，不离开列表、不压缩表格。
- Drawer 展示：
  - 记录状态。
  - 各字段最终值。
  - 字段来源。
  - 查看证据。
  - 来源页面。
  - 提取方式。
  - 提取器版本。
  - 置信度。
- 字段证据继续关联 `FieldEvidence` 与 `PageSnapshot`。

## D-042：待复核支持人工修正、通过、拒绝与 Agent 重新处理

- 状态：已确认
- 日期：2026-08-10
- 用户可以：
  - 手工修正字段值。
  - 确认通过。
  - 拒绝记录。
  - 让 Agent 重新判断/重新处理。
- 人工修正必须保留：
  - 原始提取值。
  - 原始证据。
  - 最终人工值。
  - `value_source = USER_OVERRIDE`。
  - 操作者和时间。
- 禁止覆盖/篡改 PageSnapshot 与 FieldEvidence。
- 正式导出使用最终确认值，但原始事实和人工变更历史可追溯。

## D-043：任务通过聊天结束总结卡收口，不强制跳页

- 状态：已确认
- 日期：2026-08-10
- 正常完成总结展示：
  - 已通过、待复核、已拒绝。
  - 来源/网页处理数量。
  - 完成原因。
  - 查看数据、查看质量报告、导出 CSV。
- 部分完成总结展示：
  - 当前结果数量。
  - 未达到目标或未覆盖范围。
  - 失败/未处理项目。
  - 停止原因。
  - 查看已有数据、查看未完成项、调整设置并继续。
- 探索式任务不强行展示虚假精确完成度百分比。

## D-044：Task 顶部仅保留“对话 / 数据 / 质量”三个工作区

- 状态：已确认
- 日期：2026-08-10
- 路由：
  - 对话：`/tasks/:taskId/chat`
  - 数据：`/tasks/:taskId/data`
  - 质量：`/tasks/:taskId/quality`
- 不作为一级 Tab：
  - 执行。
  - Plan。
  - Evidence。
  - Approval。
  - Logs。
  - Files。
- 原则：
  - Chat 负责人与 Agent 协作。
  - Data 负责结果浏览、审核、导出。
  - Quality 负责质量诊断。

## D-045：登录后进入轻量 Agent 工作台

- 状态：已确认
- 日期：2026-08-10
- 路由：`/app`
- 工作台包含：
  - 新任务自然语言输入。
  - 添加网址。
  - 使用模板。
  - 快速开始。
  - 运行中数量。
  - 待处理数量。
  - 最近任务。
- 行为：
  - 工作台直接发送需求 → 创建 Task Draft、保存第一条消息、进入 `/tasks/:id/chat` 并开始 Agent 目标理解。
  - `+ 新任务` → 创建空白 Task Draft → `/tasks/:id/chat`。
  - 最近任务 → 对应 Task Chat。
  - 已通过数量 → 对应 Data 已通过分区。
  - 待复核数量 → Data 待复核分区。
  - 运行中 → `/tasks?view=running`。
  - 待处理 → `/tasks?view=needs_action`。

## D-046：建立标准“我的任务”管理页

- 状态：已确认
- 日期：2026-08-10
- 路由：`/tasks`
- 能力：
  - 搜索。
  - 筛选。
  - 排序。
  - 状态操作。
- 核心视图：
  - 全部。
  - 运行中。
  - 待处理。
  - 已暂停。
  - 已完成。
  - 更多可含失败、已取消、已删除。
- 每行展示：
  - 任务名称。
  - 当前状态。
  - 当前阶段。
  - 已通过/待复核数量。
  - 最近活动。
- 点击：
  - Task 主体 → `/tasks/:id/chat`
  - 已通过数量 → `/tasks/:id/data?status=passed`
  - 待复核数量 → `/tasks/:id/data?status=review`
- `···` 根据后端 `allowed_actions` 动态展示打开、重命名、暂停、继续、导出、取消、删除等。
- 前端不得只凭字符串状态自行推断所有关键动作是否合法。

## D-047：模板保存 `CollectionSpec` 规格骨架，不只是 Prompt

- 状态：已确认
- 日期：2026-08-10
- 路由：`/templates`
- 模板保存：
  - 任务类型。
  - 目标模板。
  - 变量。
  - 字段 Schema。
  - 字段类型/必填规则。
  - 完成条件。
  - 高级运行设置。
  - 字段扩展策略。
  - 可选默认模型配置。
- 模板不保存某次任务的运行状态、数据和 Checkpoint。
- 操作：
  - 使用。
  - 编辑。
  - 复制。
  - 重命名。
  - 设为常用。
  - 删除。
- 使用模板：填写变量 → 创建 Task Draft → 引用 Template Version → `/tasks/:id/chat`。
- 从成功任务保存模板时，默认把“深圳”这类单次条件变量化为 `{city}`。
- 不开放拖拽式 Workflow Builder。

## D-048：第一版独立页面固定为 13 类

- 状态：已确认
- 日期：2026-08-10
- 原则：第一版尽量不再增加独立页面，优先使用现有页面、Drawer、Modal、Popover、折叠区和 Deep Link。
- 页面与路由：

| 编号 | 页面 | 路由 |
|---|---|---|
| 01 | 登录 | `/login` |
| 02 | 注册 | `/register` |
| 03 | 工作台 | `/app` |
| 04 | 我的任务 | `/tasks` |
| 05 | 模板列表 | `/templates` |
| 06 | 模板编辑 | `/templates/new`、`/templates/:templateId/edit` |
| 07 | 模型配置 | `/models` |
| 08 | 设置 | `/settings` |
| 09 | Agent 对话 | `/tasks/:taskId/chat` |
| 10 | 数据 | `/tasks/:taskId/data` |
| 11 | 质量 | `/tasks/:taskId/quality` |
| 12 | 执行详情 | `/tasks/:taskId/execution` |
| 13 | 证据查看器 | `/tasks/:taskId/evidence/:evidenceId` |

- 不建立独立页面：
  - 审批中心。
  - 文件中心。
  - 日志中心。
  - 待复核页面。
  - Plan 页面。
  - 网站凭据页面。
  - 回收站页面。

## D-049：工作台“待处理”复用 `/tasks?view=needs_action`

- 状态：已确认
- 日期：2026-08-10
- 不新增 Inbox/待办中心。
- 待处理聚合：
  - 等待审批。
  - 存在待复核。
  - API Key/模型失效。
  - 用户需要决定的失败/阻塞。
- 分流：
  - 待复核 → `/tasks/:id/data?status=review`
  - 审批 → `/tasks/:id/chat?approval=:approvalId`
  - 模型问题 → `/models`
  - 任务异常 → `/tasks/:id/chat` 并定位错误/停止事件。

## D-050：点击 Task 主体永远默认进入 Chat

- 状态：已确认
- 日期：2026-08-10
- 统一规则：Task 本身 → `/tasks/:taskId/chat`。
- 不采用“自动回到上次停留页面”的不稳定入口。
- 数据、质量、待处理使用明确的专用 Deep Link。

## D-051：模型配置使用单页列表 + 新增/编辑 Drawer

- 状态：已确认
- 日期：2026-08-10
- 路由：`/models`
- 不创建 `/models/:id` 页面。
- 列表展示配置名称、Provider、Model、连接状态、默认标记。
- 操作：
  - 添加模型 → Drawer。
  - 编辑 → Drawer。
  - 更换 API Key → Drawer。
  - 测试连接 → 真实最小 Provider 调用。
  - 设为默认 → 只影响未来新任务。
  - 删除 → 检查运行任务引用后允许/阻止。
- API Key 只能写入/更换，前端不可回读明文，不提供“显示 API Key”。
- 测试结果可区分：`AVAILABLE`、`AUTH_FAILED`、`MODEL_NOT_FOUND`、`RATE_LIMITED`、`NETWORK_ERROR`。

## D-052：设置页只保留四类明确设置

- 状态：已确认
- 日期：2026-08-10
- 路由：`/settings`
- 四个区：
  1. 账户资料。
  2. 安全。
  3. 采集默认值。
  4. 存储与数据。
- 账户资料：邮箱、显示名称（可选）。
- 安全：修改密码、当前会话、退出其他设备、退出登录、已保存网站凭据。
- 采集默认值：默认模型、字段扩展默认行为、高级运行默认值。
- 存储与数据：任务/记录/证据/导出统计、清理已删除任务文件、删除全部数据等危险操作。
- 不暴露 PostgreSQL、MinIO/S3、Temporal 等部署基础设施地址。

## D-053：第一版认证只做邮箱 + 密码

- 状态：已确认
- 日期：2026-08-10
- 登录：`/login`
- 注册：`/register`
- 注册字段：邮箱、密码、确认密码。
- 不要求用户名/昵称；显示名称以后在设置中可选填写。
- 第一版不做：
  - 邮箱验证。
  - 忘记密码。
  - reset password 页面。
  - OAuth/第三方登录。
- 注册成功：创建 User + Session → `/app`。
- 登录成功：`/app`。
- 未认证访问业务路由：重定向 `/login`。

## D-054：模板编辑使用独立全宽页面

- 状态：已确认
- 日期：2026-08-10
- 路由：
  - `/templates/new`
  - `/templates/:templateId/edit`
- 编辑内容：
  - 模板名称。
  - 任务类型。
  - 目标模板。
  - 变量。
  - 字段定义。
  - 完成规则。
  - 高级运行设置。
- 新建/编辑/复制均进入全宽编辑页。
- 保存创建或更新 Template Version。
- 历史任务不受后续模板修改影响。

## D-055：执行详情使用独立二级页面

- 状态：已确认
- 日期：2026-08-10
- 路由：`/tasks/:taskId/execution`
- 入口：
  - 聊天运行卡。
  - Task Status Drawer。
  - 错误/重试事件。
  - 失败总结。
- 不作为 Task 顶部第 4 个 Tab，也不出现在全局导航。
- 承载：
  - Plan Version、Run。
  - 阶段状态。
  - NodeRun/Attempt。
  - 重试、工具升级、熔断、重规划。
  - 模型调用技术统计。
  - 完整时间线。
- Token 只作为技术统计，不作为金额计费 UI。

## D-056：证据查看器使用独立二级页面

- 状态：已确认
- 日期：2026-08-10
- 路由：`/tasks/:taskId/evidence/:evidenceId`
- 快速核验先用 Drawer；深度审查进入证据查看器。
- 展示：
  - 来源 URL。
  - 抓取时间。
  - Snapshot 版本。
  - 页面快照。
  - 提取正文。
  - 原始内容。
  - 字段值。
  - 来源原文。
  - DOM/Selector。
  - 提取方式。
  - 提取器版本。
  - 置信度。
- 必须展示任务当时保存的 Snapshot，不能重新请求当前网页来冒充历史事实。
- Evidence 全部只读。

## D-057：审批使用 Drawer + Deep Link，不建独立审批页

- 状态：已确认
- 日期：2026-08-10
- Approval Drawer 展示：
  - 状态。
  - 动作。
  - 目标。
  - 原因。
  - 将访问/修改的数据。
  - 脱敏凭据引用。
  - 副作用。
  - 授权范围和有效期。
- 操作：批准、拒绝。
- Deep Link：`/tasks/:id/chat?approval=:approvalId`。
- Deep Link 打开 Task、定位审批消息并自动打开 Approval Drawer。

## D-058：全局左侧导航支持手动收起

- 状态：已确认
- 日期：2026-08-10
- 展开时：
  - 工作台。
  - + 新任务。
  - 我的任务。
  - 模板。
  - 模型配置。
  - 设置。
- 收起时仅图标 + Tooltip。
- 收起状态是前端 UI Preference，不属于 Task 状态。

## D-059：网站 Cookie/登录凭据按任务需要时再提供

- 状态：已确认
- 日期：2026-08-10
- 不建立独立凭据管理页面。
- 当任务遇到需要登录/非公开页面时，Chat 出现“需要凭据”卡片。
- “提供凭据”打开 Credential Drawer。
- 用户可选择仅当前 Task 使用，或保存供该域名后续使用。
- FastAPI 加密保存，Task 只保留 `credential_id`。
- Prompt/普通日志只能看到凭据引用，不能看到秘密。
- 长期保存的凭据在 `设置 → 安全 → 已保存网站凭据` 管理，只显示域名、类型、时间和删除入口。
- 真正使用非公开凭据前仍按 D-017 审批。

## D-060：数据页提供搜索、筛选、排序、列设置和 CSV 导出

- 状态：已确认
- 日期：2026-08-10
- 路由：`/tasks/:taskId/data`
- 顶部分区：已通过、待复核、已拒绝。
- 搜索：后端按 `user_id + task_id + result_partition` 分页查询。
- 筛选：
  - 字段、条件、值。
  - 来源类型。
  - 提取方式。
  - 置信度。
  - 第一版只做简单 AND，不做复杂查询构造器。
- 排序：仅展示可排序字段。
- 列设置：只影响 UI 显示，不修改 `CollectionSpec`。
- 导出：
  - 正式 CSV：默认已通过。
  - 待复核 CSV：含复核原因。
  - 审核完整 CSV：可含三类状态和状态字段。
  - 支持全部当前分区或当前筛选结果。
- 导出 Artifact 记录 `task_id`、`dataset_version`、`filter_snapshot`、`export_type`、`content_hash`。

## D-061：待复核支持受限制的批量审核

- 状态：已确认
- 日期：2026-08-10
- 后端返回：
  - `review_type`
  - `review_reason`
  - `allowed_actions`
- 只有语义兼容的记录才开放对应批量动作。
- 批量动作可包括：
  - 批量通过。
  - 批量拒绝。
  - 批量重新处理。
- 不同风险原因的记录不能被无条件“全部通过”。
- 批量重新处理仍遵循 D-013 的有效变化后重试约束。
- 审计保留 `reviewed_by`、`reviewed_at`、`reason`、`batch_operation_id`。

## D-062：质量页指标可下钻，数据修改统一回 Data

- 状态：已确认
- 日期：2026-08-10
- 路由：`/tasks/:taskId/quality`
- 展示：
  - 已通过/待复核/已拒绝。
  - 字段完整性。
  - 字段缺失。
  - 来源冲突。
  - 重复记录。
  - 低置信度。
  - 来源覆盖。
  - 抽样验证。
- 指标全部可点击下钻到 Data 的对应筛选结果。
- 示例：
  - 待复核 → `/tasks/:id/data?status=review`
  - 来源冲突 → Data + `review_type=source_conflict`
  - 企业官网 → Data + `source_type=official_site`
- Quality 只负责诊断，不复制人工修正、通过、拒绝等数据处理能力。

## D-063：执行详情默认“阶段列表 + 时间线”，流程图作为切换视图

- 状态：已确认
- 日期：2026-08-10
- 默认阶段视图展示来源发现、网页采集、字段提取、数据验证等阶段状态与数量。
- 时间线筛选：
  - 全部。
  - 错误。
  - 重试。
  - 工具升级。
  - 计划调整。
  - 模型调用。
  - 暂停/恢复。
- “流程图”切换：只读渲染当前 Plan Version DAG。
- 点击 Node 打开 Node Detail Drawer。
- Node Detail 可展示状态、节点版本、执行次数、输入、输出、最近错误、耗时、Token 等技术统计。
- 只有后端 `allowed_actions` 明确允许 Retry 时才展示人工重试按钮。

## D-064：证据查看器默认优先展示网页视觉快照

- 状态：已确认
- 日期：2026-08-10
- 默认优先级：
  1. 有网页快照/截图 → 页面快照。
  2. 无快照但有正文 → 提取正文。
  3. 只有原始内容 → 原始内容。
- “定位到页面位置”根据 DOM/Selector 自动滚动并高亮证据区域。
- 用户可搜索、复制、查看原始来源 URL。
- 用户不可修改 Snapshot、正文、HTML、FieldEvidence。
- 最终 Record 值修改仍从 Data → Record Drawer 完成。

## D-065：删除任务先进入可恢复状态

- 状态：已确认
- 日期：2026-08-10
- 删除 Task 默认进入 `DELETED`，不立即物理删除。
- 不新增回收站页面；使用 `/tasks?view=deleted`。
- 已删除视图提供：
  - 恢复。
  - 永久删除。
- 运行中的 Task 不能直接删除，必须先取消并等待运行停止。
- 永久删除二次强确认后清理 PostgreSQL 业务数据和对象存储中的网页快照、截图、正文、诊断包、CSV 等。
- 删除、恢复、永久删除都必须重新校验 D-023 的用户归属。

## D-066：没有可用模型时允许浏览和填写，真正调用 Agent 时再拦截

- 状态：已确认
- 日期：2026-08-10
- 无 ModelConfig 时仍允许：
  - 浏览系统。
  - 进入新任务。
  - 填写需求。
  - 添加网址。
  - 选择模板。
- 真正需要目标理解、生成 Spec、规划或语义提取时检查是否存在已测试可用 ModelConfig。
- 无模型时打开 Model Required Modal：
  - 提示需要配置 AI 模型。
  - “去配置模型” → `/models`。
  - 保留当前 Task Draft 和输入内容。
- 配置成功后提供“返回刚才的任务”。
- 所有入口共用 `MODEL_NOT_CONFIGURED` 处理逻辑。

## D-067：全局导航、二级页面与 Overlay 组件边界最终确定

- 状态：已确认
- 日期：2026-08-10
- 全局导航：
  - 工作台 → `/app`
  - + 新任务 → 创建 Task Draft → `/tasks/:id/chat`
  - 我的任务 → `/tasks`
  - 模板 → `/templates`
  - 模型配置 → `/models`
  - 设置 → `/settings`
- Task 顶部：
  - 对话 → `/tasks/:id/chat`
  - 数据 → `/tasks/:id/data`
  - 质量 → `/tasks/:id/quality`
- 二级页面：
  - 执行详情 → `/tasks/:id/execution`
  - 证据查看器 → `/tasks/:id/evidence/:evidenceId`
- Overlay / Modal：
  - Task Status Drawer。
  - `CollectionSpec` Editor Sheet。
  - Approval Drawer。
  - Credential Drawer。
  - Record Detail Drawer。
  - Evidence Quick Drawer。
  - Node Detail Drawer。
  - Model Add/Edit Drawer。
  - Template Variable Modal。
  - Export Modal。
  - Delete Confirm Modal。
  - Model Required Modal。
- UI 统一规则：
  - Drawer 使用 Overlay，不挤压底层页面。
  - 与 Task/Record/Node/Approval 强上下文相关的能力优先 Drawer/Deep Link，而不是新页面。
  - 暂停、恢复、取消、删除、重试、批量审核、审批、导出等必须绑定真实后端命令/状态校验。
  - 关键动作优先由后端 `allowed_actions` 或等价命令校验驱动。

## 当前 UI/UX 设计阶段结论

- 状态：已确认
- 日期：2026-08-10
- 第一版独立页面固定为 13 类。
- 产品主形态：Agent 对话驱动。
- Task 核心工作区：对话 / 数据 / 质量。
- 技术深度信息：执行详情页、证据查看器和 Overlay Drawer 承载。
- 第一版原则：除非现有页面无法承载且存在明确业务必要性，否则不再新增独立页面。
- 下一阶段可以直接进入：
  1. 各页面高保真线框稿与视觉规范。
  2. Vue 3 路由与组件树拆分。
  3. 页面交互到 FastAPI 命令/查询/SSE 的接口矩阵。
  4. `allowed_actions`、Deep Link、Drawer 状态与前端 Store 设计。


## D-068：探索式来源发现采用“外部搜索 + 站内 URL 扩展”两阶段

- 状态：已确认
- 日期：2026-08-10
- 维度：来源发现、网页抓取与错误恢复
- 决定：探索式和混合式任务的来源发现分为两层，不依赖浏览器 Agent 在互联网中自由漫游。
- 第一层：外部来源发现。
  - 使用 Search Provider、用户提供的 Seed URL、当前任务可用的已知入口等发现候选站点或候选入口页。
  - 外部搜索的主要目标是发现“值得继续分析的网站/入口”，而不是把搜索结果页本身直接当成最终数据集。
- 第二层：站内 URL 扩展。
  - 对候选站点执行访问规则检查。
  - 优先读取 sitemap、RSS、导航链接、栏目页、分页链接和页面内符合规则的链接。
  - 将发现的 URL 进入站内 URL Frontier，再根据 `CollectionSpec`、页面类型、去重规则和执行策略判断是否抓取。
- 节点关系：该策略落入现有标准节点体系，典型路径为 `SourceSearch → AccessRulesCheck → LinkDiscovery → Fetch`。
- 浏览器 Agent：仍作为最后一级能力，只在普通搜索、HTTP、结构化链接发现和浏览器渲染不足以完成任务时按 D-009 升级使用，不作为默认来源发现器。
- 影响：搜索负责“找到站点”，Crawler 负责“在站内找到真正有价值的页面”，提高来源覆盖和可恢复性，同时保持抓取边界可控。

## D-069：外部搜索能力采用 BYOK + 可插拔 Search Provider

- 状态：已确认
- 日期：2026-08-10
- 维度：来源发现与外部服务接入
- 决定：外部搜索能力不由平台第一版统一承担，而采用与模型接入类似的 BYOK + 适配器模式；Search Provider 与 Model Provider 是不同的配置类型和执行契约。
- 任务依赖：
  - 指定来源抽取任务不要求 Search Provider，可直接基于用户提供的 URL/站点运行。
  - 探索式和混合式任务在真正启动来源搜索前必须存在已配置且可用的 Search Provider。
  - 缺少搜索配置时任务保持草稿/待处理上下文，提示用户配置搜索服务，不静默降级为抓取搜索引擎结果页。
- 适配器：首版通过统一 `SearchProvider` 接口接入第三方搜索 API 或自定义兼容搜索服务，后续新增 Provider 不改变 Agent 主流程。
- 密钥：Search Provider API Key 与模型 Key 一样使用用户私有加密凭据引用，不进入 Prompt、普通日志或跨用户共享。
- 页面边界：不新增独立页面；复用现有 `/models` 配置页面，在其中增加与“AI 模型”并列的“搜索服务”配置区/Tab，新增与编辑仍使用 Overlay Drawer。
- 平台扩展：未来若平台提供公共搜索能力，可作为系统级 Provider 接入同一契约，不修改任务业务逻辑。

## D-070：默认遵守 robots.txt，公开页面允许用户显式覆盖

- 状态：已确认
- 日期：2026-08-10
- 维度：站点访问边界
- 决定：系统默认读取并遵守 robots.txt 与可检测的站点访问规则；命中 `Disallow` 时默认不抓取对应路径。
- 覆盖条件：用户可对“公开、无需登录、无需绕过技术访问控制”的页面做显式覆盖；覆盖必须展示风险提示，并记录任务、域名/路径、操作者、时间、原因、规格版本和适用范围。
- 禁止覆盖：以下情况不能通过普通 robots 覆盖授权放行：
  - 登录墙或需要身份认证的非公开内容。
  - 验证码绕过。
  - 绕过鉴权、访问控制或安全策略。
  - 利用漏洞、伪造凭据或其他被 D-009、D-017 禁止的行为。
- 凭据访问：需要用户合法凭据访问非公开页面时，仍必须走已确认的 Credential + Approval 流程，不能用 robots 覆盖替代审批。
- 执行记录：访问规则检查、默认阻止、用户覆盖和最终抓取决策都进入任务时间线与审计事件。

## D-071：任务执行采用全局限制、用户限制和节点资源池三级调度

- 状态：已确认
- 日期：2026-08-10
- 维度：任务调度与执行资源
- 决定：Temporal Workflow 可以大量存在，但网络、Scrapy、浏览器、模型等资源密集 Activity 不能无限并发；第一版使用三级调度边界。
- 全局层：限制整个部署实例允许同时活跃/执行的任务或资源总量，防止服务器整体过载。
- 用户层：限制单个用户同时占用的活跃任务/执行资源，避免某个账号独占系统容量。
- 节点资源池：按能力拆分 Worker Pool / Temporal Task Queue，并独立控制并发，例如：
  - HTTP Fetch：高并发资源池。
  - Scrapy：中高并发资源池。
  - Playwright / Browser：低并发资源池。
  - LLM Call：独立并发与 Provider 限速资源池。
- 配置边界：具体并发数属于部署/运维配置，根据机器规格和 Provider 限额调整，不写死进 `CollectionSpec`，也不暴露给普通用户自由修改。
- 等待语义：资源槽位不足时节点进入明确的等待资源状态（如 `WAITING_RESOURCE` 或等价内部状态），不得把资源等待误报为卡死或失败。
- UI：用户只看到“等待可用执行资源”等可理解状态；不需要理解 Worker Pool、Task Queue 等基础设施细节。
- 原则：用户决定“采什么”，系统根据当前部署容量决定“安全地同时跑多少”。

## D-072：业务事实长期保留，重型原始文件使用引用安全的生命周期清理

- 状态：已确认
- 日期：2026-08-10
- 维度：数据保留与对象存储生命周期
- 决定：数据保留区分“长期业务事实”和“高体积原始文件”，不采用所有内容永久无限增长，也不在任务完成后立即删除证据。
- 长期业务事实至少包括：
  - Task、`CollectionSpec`、Plan、Run 及版本关系。
  - 最终 Record 与审核结果。
  - FieldEvidence 索引及支撑最终值所需的必要原文片段/定位信息。
  - 质量报告与关键质量指标。
  - 状态事件、审批事件和必要执行审计信息。
  - CSV / Artifact 元数据与内容校验标识。
- 可配置生命周期的高体积文件包括：
  - 完整 HTML。
  - 完整正文文件。
  - 页面截图和浏览器快照。
  - 诊断包。
  - 临时下载和其他可重建中间文件。
- 生命周期值：默认保留期属于部署配置，可按环境设置，例如 30 天、90 天等；当前业务决定不写死具体天数。
- 引用保护：生命周期清理前必须检查对象是否仍被有效 FieldEvidence、Record、质量审核或其他需要保留的业务事实引用；仍承担证据作用的对象不得被普通生命周期策略删除。
- 明确永久删除：用户在 D-065 的“永久删除任务/全部数据”流程中完成强确认后，可按删除策略清理对应业务数据和对象文件；该显式删除与普通生命周期清理是不同语义。
- UI：用户只在 `设置 → 存储与数据` 查看存储概要、主动清理和删除入口，不暴露 MinIO/S3 等底层实现。
- 影响：控制对象存储长期膨胀，同时保证待复核、质量审查和字段证据链不会因为自动清理而失效。

## 当前执行与运维补充设计阶段结论

- 状态：已确认
- 日期：2026-08-10
- 本轮 D-068～D-072 补齐第一版落地所需的关键执行边界：
  1. 来源发现采用“外部搜索发现站点 + 站内 URL 扩展”的两阶段策略。
  2. Search Provider 使用 BYOK + 可插拔适配器，并复用现有配置页面，不新增独立页面。
  3. robots.txt 默认遵守，只有公开页面可以在明确风险提示后由用户显式覆盖，访问控制和验证码等禁止边界不受影响。
  4. 执行调度使用全局、用户和节点资源池三级限制，具体容量由部署配置决定。
  5. 结构化业务事实长期保留，高体积原始文件按引用安全的生命周期策略清理。
- 本阶段收束原则：后续 API 字段、告警阈值、测试矩阵和具体部署数值优先从已确认决策推导，不再为非关键参数逐项扩大产品需求讨论。

## D-073：模型 Provider 支持安全自动识别、托管 Base URL 与连接耗时测试

- 状态：已确认
- 日期：2026-08-13
- 维度：模型 Provider 配置交互与安全边界
- 决定：在 D-051 的「模型配置 Drawer」基础上新增「检测连接」能力，并在 Model Provider 未保存前安全识别 Provider 类型、自动解析 Base URL、测量真实连接耗时；**不替代 D-069 的 Model/Search 独立边界**。
- 扩展 D-051 交互：
  - 「新增模型」打开「新增 AI 模型」Drawer，表单顺序建议为：配置名称 → API Key → 检测连接 → Provider → Model → 高级设置。
  - 检测连接可在保存前使用，成功后自动回填 Provider 并展示「连接成功 · Provider · xxx ms」；失败/无法识别时只展示稳定安全文案。
- Base URL 模式（以 Backend Provider Registry 为事实来源，不在前端硬编码 URL 表）：
  - `managed`：内置 Provider（OpenAI、DeepSeek、OpenRouter、Anthropic、Gemini）的官方/default endpoint 由 Registry 管理，普通用户无需输入 Base URL，可在高级设置只读查看解析后的地址。
  - `required`：`custom_openai_compatible` 需要用户输入 Base URL，且 Base URL 做合法 URL 校验。
  - `local_required`：Ollama 无需 API Key，但需要用户输入 Base URL，仍可测试连接并返回耗时。
- Probe 安全策略（`POST /api/providers/models/probe`，未保存配置也能调用）：
  - 接收当前 Drawer 未保存的 API Key，采用 `SecretStr` 写入；**不创建 ModelConfig、不创建 Credential、不持久化 API Key**。
  - 阶段 1（Key fingerprint）：Registry 维护确定性的 Key 格式 matcher，只在高置信度时识别，返回 `HIGH` / `AMBIGUOUS` / `NONE`；不用过宽 Regex 猜测。
  - 阶段 2（Single-provider probe）：仅当 fingerprint 得到单一高置信度 Provider，或用户明确手工选择 Provider，才真正发起一次真实、最小、低成本的 Provider 请求。
  - **一次 Probe 最多把 API Key 发送给一个外部 Provider**；绝不把 Key 依次广播给 OpenAI/Anthropic/DeepSeek/OpenRouter/Gemini 看哪个成功。
  - Provider 无法仅凭 Key 可靠识别（如通用 `sk-` 同时可能为 OpenAI/DeepSeek）时，返回 AMBIGUOUS，前端提示「无法仅根据 API Key 唯一识别服务商，请选择 Provider 后重新测试」，要求用户选择而非猜测。
- 测试结果 DTO：在 `ProviderTestResult` 稳定状态（AVAILABLE / AUTH_FAILED / MODEL_NOT_FOUND / RATE_LIMITED / NETWORK_ERROR / FAILED）基础上增加 `detected_provider`、`detection_confidence`、`resolved_base_url`、`latency_ms`（可选 `probe_method`）；错误正文不回传浏览器，只返回稳定 `error_code` 与简短安全 message。
- 耗时定义：`latency_ms` 为发起真实 Provider probe 到收到可判断结果之间的耗时（monotonic timer），非精确网络 ping，超时有界，本轮一次最小真实请求即可，不为测速多次取平均。
- 安全：Probe 完成后 API Key 不落库、不写普通日志、不写 DomainEvent/Outbox/Temporal History/OpenTelemetry attribute/错误 message，也不回传前端；原有 CredentialVault 信封加密、Key rotate/revoke、`/models/{id}/test` 已保存配置测试、M-06 ModelInferenceClient 与 M-09 Search Provider 均保持兼容不被破坏。
- 影响：Model Provider 与 Search Provider 继续使用完全独立的配置类型、DTO、Provider definitions 与 API 调用链；Drawer 内部明确拆分 Model/Search 上下文（`kind = model | search`），关闭后清理 create/edit context，不因当前 Tab 或上一次 Drawer 状态串线。

## D-074：Search Provider 保存前连接测试与按 Registry 驱动的字段校验

- 状态：已确认
- 日期：2026-08-13
- 维度：Search Provider 配置交互与安全边界
- 背景：D-073 已为 Model Provider 建立保存前 Probe 与托管 Base URL 模式；D-069 明确 Search Provider 与 Model Provider 是完全独立的配置类型与执行契约。本轮修复 Tavily 等内置 Search Provider 在「新增搜索服务」时被错误要求 Base URL 的 Bug，并把同样的安全 Probe 能力补到 Search 侧。
- Bug 修复部分（属于现有规则错误实现，不作为新需求伪造）：
  - Tavily 是内置 Search Provider，`requires_api_key = true`、`requires_base_url = false`；Base URL 由 Adapter / Registry 管理（`https://api.tavily.com`），用户无需也不得被要求填写 Base URL。
  - 根因：前端 `SearchConfigDrawer` 无条件校验 `base_url` 非空（Base URL 输入框却按 `requires_base_url` 条件渲染），后端 `Create/UpdateSearchConfigCommand.base_url` 被声明为必填 `str`。两者都必须改为「按 Provider Definition 校验」，不允许只隐藏输入框、后端仍 required，也不允许只删文案。
  - 字段需求唯一事实来源 = Backend Provider Registry（`GET /api/providers/definitions`），前端按 metadata 动态显示与校验，不在 Vue 硬编码 URL 表或 per-provider if/else。
- 新产品行为：未保存 Search Provider 可测试连接（`POST /api/providers/searches/probe`）：
  - 请求体 `SearchProbeCommand`：`provider_type`（必填，Search 无 Key fingerprint 阶段，用户始终显式选择 Provider）、`api_key`（`SecretStr`）、`base_url`（可选，仅 custom 使用）。
  - Probe 安全策略与 D-073 一致：API Key 只经 HTTP body → Pydantic `SecretStr` → Provider Adapter 临时使用；**不创建 SearchConfig / Credential / CredentialVersion、不写 PostgreSQL / DomainEvent / Outbox / Temporal History / 普通日志 / OTel attributes / 错误 message，也不回传前端**。只有用户点击「保存」才进入既有 CredentialVault 加密存储流程。
  - 必须是真实、最小、低成本请求（如 Tavily 官方 `POST /search`，`max_results=1` 的 harmless query），不得用「Key 非空 → 连接成功」冒充。
  - 复用 `ProviderTestResult` 稳定状态（AVAILABLE / AUTH_FAILED / RATE_LIMITED / NETWORK_ERROR / FAILED），在其基础上返回 `provider_type`、`resolved_base_url`、`latency_ms`（monotonic timer，含第三方处理时间，非纯网络 ping）；错误正文不回传，只返回稳定 `error_code` 与简短安全 message。
  - Search Provider 字段需求：`tavily` = managed（Registry 管 endpoint，无 Base URL 输入）；`custom_compatible_search` = required（用户必须填合法 http/https Base URL，不接受空串）。create/update 保存校验同样按 Definition 执行。
  - 测试连接不是保存配置的硬门禁：用户可跳过测试直接保存；配置状态沿用现有语义（如 `untested`）。但字段必填要求仍按 Definition 强制执行。
  - 已保存配置的测试继续复用 `POST /api/providers/searches/{config_id}/test`；新增配置未保存时无 config_id，才使用 probe。
- 前端 Drawer 交互：
  - Tavily 表单：配置名称 → Provider → API Key → 高级设置只读 Base URL → [测试连接] → 结果区 → [取消][保存]；不显示 Base URL 必填框。
  - 测试按钮状态机：`idle / testing / success / error`；testing 时禁止重复点击、不清空已填 API Key、不保存配置；成功显示「连接成功 · Tavily · xxx ms」，认证失败显示「API Key 无效」等稳定安全文案。
  - 修改 API Key / Provider / Base URL 后，上一次测试结果必须立即失效，不得出现「Key B 仍显示 Key A 测试成功」。
  - 编辑已有配置时 API Key 不可回读（显示「已配置」），未输入新 Key 不得清空旧 Key；输入新 Key 走既有 rotate / credential version 流程。
- 影响：不替代 D-069 的 Model/Search 独立边界；未来新增 Brave / Serper / SerpAPI / Bing-compatible / Custom Search 只需在 Registry 注册，前端字段与校验自动适配。

## D-075：模型 ID 必须来自 Provider 实时目录

- 状态：已确认
- 日期：2026-08-14
- 维度：模型 Provider 配置交互、真实推理与错误前移
- 决定：用户选择具体 Model Provider 后，Kairos 必须通过该 Provider 的官方模型目录接口取得当前可用模型 ID；「模型名称」使用下拉选择，不再允许自由文本输入。
- 新增配置使用本次表单中的 write-only API Key 获取目录；已有配置通过 owner-scoped `config_id` 使用其当前 CredentialVersion，并且只允许 `CredentialVault.read_for_execution` 解密。
- 已有 CredentialVersion 只能用于其原 Provider；编辑配置不得切换 Provider 并复用旧凭证。更换厂商必须新建配置，防止凭证被误发给另一厂商或自定义端点。
- 保存后的「测试连接」必须验证所选模型仍存在于同一 Registry / Adapter / Base URL / Credential 解析链返回的目录中。仅 Provider 端点可达不能再标记为模型配置 `AVAILABLE`。
- 模型目录加载失败时必须显示真实失败状态和重试入口；Production 禁止静默使用硬编码、样例或上次缓存的假模型列表。
- Model Provider 与 Search Provider 保持分离；本决定不改变 Tavily 的使用阶段。
- 原因：用户不应掌握或猜测厂商模型 ID。自由输入会让错误值通过浅层连接测试，并在真实 Goal Understanding 才失败。
- 关联：D-029、D-051、D-066、D-069、D-073；M-03、M-06。

